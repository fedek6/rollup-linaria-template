import * as React from "react";
import { css } from "@linaria/core";

const header = css`
  text-transform: uppercase;
`;

// Delete me
export const HelloWorld = () => {
  return <h1 className={header}>the snozzberries taste like snozzberries</h1>;
};
